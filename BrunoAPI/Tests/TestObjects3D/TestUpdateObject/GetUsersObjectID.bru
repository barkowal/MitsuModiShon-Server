meta {
  name: GetUsersObjectID
  type: http
  seq: 1
}

get {
  url: {{local}}:3000/api/v1/objects3D/private?per_page=10&animated=false
  body: none
  auth: inherit
}

params:query {
  per_page: 10
  animated: false
}

script:post-response {
  let existingID = res.getBody().data.result.objects[0].id;
  bru.setVar("existingObjectID", existingID);
}

settings {
  encodeUrl: true
}
