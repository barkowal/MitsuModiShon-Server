meta {
  name: TryUpdateNullObject
  type: http
  seq: 4
}

patch {
  url: {{local}}:3000/api/v1/objects3D/-1
  body: json
  auth: inherit
}

body:json {
  {
    "name":"newUpdatedName",
    "isPublic":true
  }
}

assert {
  res.status: eq 404
}

tests {
  test("should return error", function () {
    let res_error = res.getBody().error;
    expect(res_error).to.be.string;
  });
}

settings {
  encodeUrl: true
}
