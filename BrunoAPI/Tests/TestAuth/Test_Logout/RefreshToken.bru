meta {
  name: RefreshToken
  type: http
  seq: 2
}

get {
  url: {{local}}:3000/api/v1/auth/refresh
  body: none
  auth: inherit
}

tests {
  test("should return access token in cookies", function () {
    const accessToken = res.getHeader('set-cookie')[0];
    expect(accessToken).to.not.be.null;
  });
}

settings {
  encodeUrl: true
}
