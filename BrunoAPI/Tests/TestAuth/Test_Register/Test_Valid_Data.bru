meta {
  name: Test_Valid_Data
  type: http
  seq: 1
}

post {
  url: {{local}}:3000/api/v1/auth/register
  body: json
  auth: inherit
}

body:json {
  {
    "username":"Chris",
    "email": "{{$randomEmail}}",
    "password":"passsword"
  }
}

assert {
  res.status: eq 201
  res.body: isJson
}

settings {
  encodeUrl: true
}
