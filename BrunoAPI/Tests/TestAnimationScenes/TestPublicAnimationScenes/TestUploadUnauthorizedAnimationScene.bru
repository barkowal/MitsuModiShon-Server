meta {
  name: TestUploadUnauthorizedAnimationScene
  type: http
  seq: 5
}

post {
  url: {{local}}:3000/api/v1/animationScene/upload
  body: multipartForm
  auth: inherit
}

body:multipart-form {
  sceneData: {"name":"test_scene","is_public":true,"duration":120}
  scene: @file(testFiles/test_scene.json)
  image: @file(testFiles/test_scene_render.png)
}

assert {
  res.status: eq 401
}

settings {
  encodeUrl: true
  timeout: 0
}
